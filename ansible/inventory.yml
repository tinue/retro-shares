---
all:
  children:
    retro_servers:
      hosts:
        retro-server.example.com
        # Add your server(s) here
        # Example:
        # retro-server-1:
        #   ansible_host: 192.168.1.100
        #   ansible_user: admin
        #   ansible_ssh_private_key_file: ~/.ssh/id_rsa

        # Local testing with Vagrant or local Docker host:
        # localhost:
        #   ansible_connection: local
